{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bun",
            "request": "launch",
            "name": "dev",
            // "runtimeExecutable": "bun",
            "runtimeArgs": ["run", "--hot", "src/index.ts"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
