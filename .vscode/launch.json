{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bun",
            "request": "launch",
            "name": "dev",
            "program": "${workspaceFolder}/src/index.ts",
            "cwd": "${workspaceFolder}",
            // "runtimeArgs": ["--inspect-brk"]
        }
    ]
}
